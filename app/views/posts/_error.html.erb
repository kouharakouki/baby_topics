<div class="row justify-content-center alert-text">
  <div class="col-auto mx-auto">
    <% if post.errors.any? %>
      <div><%= post.errors.count %>件のエラーが発生しました</div>
      <% post.errors.full_messages.each do |message| %>
        <div>※<%= message %></div>
      <% end %>
    <% end %>
  </div>
</div>